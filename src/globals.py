# Global variables 

# Imports


# Variables

config_path = ""
# What config should have:
# command_prefix
# description
# token
# database_folder_path

logging_enabled = True
# Logging master switch

permissions_default_type = "Whitelist" 
# Selects the default permissions, Whitelist means that by default, nightly fluffy manager will deny any commands if
# you're not manually allowed to execute it, Blacklist means that by default you will be able to execute anything unless
# manually specified otherwise